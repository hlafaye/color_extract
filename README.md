# 🎨 Color Palette Extractor from Image

A small web application built with **Flask** that extracts a color palette from an image using **K-Means clustering**.  
Upload an image, choose the number of colors, and instantly get a clean, clickable palette (hex codes copied to clipboard).

---

## 🚀 Features

- 📷 Upload an image (JPG / PNG)
- 🎯 Choose the number of colors to extract
- 🧠 Color extraction using **K-Means (scikit-learn)**
- 🎨 Display colors with live preview
- 📋 Click a color to **copy HEX code** to clipboard
- 🌙 Dark-mode UI (Bootstrap 5)
- ⚡ Fast local processing (no external API)

---

## 🛠️ Tech Stack

**Backend**
- Python 3
- Flask
- scikit-learn
- NumPy
- Pillow
- python-dotenv

**Frontend**
- HTML / Jinja2
- Bootstrap 5
- Vanilla JavaScript
- CSS (custom dark UI)

---

## 📂 Project Structure

# 🎨 Color Palette Extractor from Image

A small web application built with **Flask** that extracts a color palette from an image using **K-Means clustering**.  
Upload an image, choose the number of colors, and instantly get a clean, clickable palette (hex codes copied to clipboard).

---

## 🚀 Features

- 📷 Upload an image (JPG / PNG)
- 🎯 Choose the number of colors to extract
- 🧠 Color extraction using **K-Means (scikit-learn)**
- 🎨 Display colors with live preview
- 📋 Click a color to **copy HEX code** to clipboard
- 🌙 Dark-mode UI (Bootstrap 5)
- ⚡ Fast local processing (no external API)

---

## 🛠️ Tech Stack

**Backend**
- Python 3
- Flask
- scikit-learn
- NumPy
- Pillow
- python-dotenv

**Frontend**
- HTML / Jinja2
- Bootstrap 5
- Vanilla JavaScript
- CSS (custom dark UI)

---

## 📂 Project Structure

colors_extract/
├── .venv/ # Virtual environment (ignored)
├── static/
│ ├── style.css
│ └── app.js
├── templates/
│ └── colors_images.html
├── .env # Environment variables (ignored)
├── colors_images.py # Flask app
├── requirements.txt
└── README.md



---


Frontend kept lightweight on purpose (no framework)

👤 Author
Built by Hugo Lafaye
Python • Flask • Data • Automation

